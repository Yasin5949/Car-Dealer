# Set default behavior for all files
* text=auto

# Declare files that will always have LF line endings on checkout
*.sh text eol=lf
*.js text eol=lf
*.html text eol=lf
*.css text eol=lf

# Declare files that will always have CRLF line endings on checkout
*.bat text eol=crlf
*.ps1 text eol=crlf

# Ensure that binary files are treated correctly
*.jpg binary
*.png binary
*.gif binary

# Handle specific file types
*.md text
*.json text
*.yml text
